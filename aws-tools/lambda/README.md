# Lamda function to stop OpenShift Clusters in a region

This is a Lambda function that goes through each region in AWS and shutdown the OpenShift clusters.  We do this to try to cut down costs for AWS.
